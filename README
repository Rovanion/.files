My own config files.

In order to setup run:

$ git clone https://github.com/Rovanion/.files.git
$ cd .files
$ ./setup.bash

The config files should now be in your home directory.nsatheousntheoasnuthoaesnuthaosnteuh